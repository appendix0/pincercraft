# Abort log

Every discarded run or attempt, written **at the time of the abort**.
Governed by §8 of [preregistration.md](preregistration.md): a run may be discarded
only for a declared infrastructure fault (rules 1–4), never because the result
looked wrong.

The discard rate computed from this file is reported in the paper.

| Date (UTC) | Run tag | Rule | Cause | Attempts lost |
|---|---|---|---|---|
| 2026-08-07 | `bench_measurement` seed 2 | 3 | **Anthropic API credit exhausted mid-campaign.** Every LLM call returned `400 ... "Your credit balance is too low to access the Anthropic API"`. The bot stayed connected and received drive nudges, but took 0 turns; all 10 attempts ran to the full 480 s timeout with inventory unchanged. Attempts #490–#499. Detected 2026-08-08 while investigating the layer's 61.1 pp drop. | 10 |
| 2026-08-09 | `conf_*` backtest 1 | — | **Campaign interrupted by Anthropic API credit exhaustion, no attempts lost.** Stopped after 33 of 364 runs, partway through the 3rd of 7 arms. The credit error appears in `bot.log` only from line 122714, inside attempt #710 which was in flight and never logged; the campaign window (119400–122713) contains **zero** credit errors, and all 33 collected attempts have non-zero step counts. Nothing is discarded. `conf_on` holds 7 of 13 tasks and must be re-run from scratch on resume, since arms are never mixed across partial task sets. **Superseded 2026-08-12 — see the row below; the whole segment was discarded and backtest 1 re-run.** | 0 |
| 2026-08-12 | `conf_gates` (13) / `conf_off` (7), backtest 1, second launch | 3 (run-level) | **Collection restarted after a health-check defect was found 20 attempts in.** The runner treated any 0-step attempt as a bot unable to act, but attempt #725 (`conf_off`) claimed `outcome=done` 0.27 s after its task was queued with cobblestone 96 → 96 — a genuine false completion, scored `false_done_referee`. Two such in a row would have restarted the bot mid-arm, and only in arms where the grounded completion check is ablated, making the runner react to the endpoint it measures. Fixed to require 0 steps **and** no completion claim. The 20 attempts are individually valid and no spurious restart actually fired, but they ran under the pre-fix runner, and §8 forbids mixing arms across code versions. Rows retained in `exclusions`, not deleted. | 20 |
| 2026-08-12 | `conf_gates` / `conf_off` / `conf_on` backtest 1 | 3 (run-level) | **Whole interrupted segment discarded on resume; backtest 1 re-run from scratch.** Rule 3 covers the *run*: the runner terminated mid-backtest on credit exhaustion. The 33 individual attempts were clean, so this is a run-level discard of a segment that could not be completed without corrupting a covariate — not an attempt-level fault, and explicitly not because any result looked wrong (`gates` 12/13, `off` 5/13, `on` 5/7 at the time of the decision; the direction of all three is unchanged by discarding them). The 2026-08-09 row above planned to keep `conf_gates` (13) and `conf_off` (13) and re-run only `conf_on`. That plan was reversed before any resume run, on two grounds that are independent of the results. (1) **Arm position would have been recorded falsely.** `field_trial.sh` numbers `ARM_POSITION` from 1 within each invocation, but the 5 outstanding arms truly occupy positions 3–7 of backtest 1's randomized order (`gates off on reflexes perception verify autofinish`). Recording them as 1–5 corrupts a covariate the campaign exists to control; the alternatives were editing the frozen runner mid-campaign or hand-patching receipt rows, both worse. (2) **It would have given `gates` and `off` a unique 13/52 share of pre-pause data** across a 3-day gap, while the other five arms sat entirely post-pause. `gates` is the arm carrying the unresolved 100%-success anomaly, so a confound touching only that arm is the one least affordable. Cost of discarding: 26 valid attempts, ≈1.4 h of a ≈15.6 h campaign. Attempts are retained in the database and marked in `exclusions`; nothing is deleted. | 33 |
| 2026-08-08 | `bench_on` seed 3 | 3 | **Agent wedged holding the action-execution lock.** After #516 the log repeats `waiting for code to finish executing...` indefinitely with `executing=true` and no active task; every subsequent task was cancelled in 0–30 s at 0 steps. Attempts #517–#526. The runner restarts the bot only between arms, so one wedge poisoned the rest of the arm. | 10 |

| 2026-08-13 | `conf_off` backtests 2 and 4 | 3 (runner defect) | **Task-queue dedup collision — two phantom rows on the primary endpoint.** The runner requested the next benchmark, but the task-giver's add matched an ALREADY-COMPLETED task and was handed back its id, so the attempt reported `done` in ~0 s without running and logged a second row carrying the *previous* task's name. Signature: the same `task_id` logged twice (visible as one "tier 1" row and one "tier 4" row), zero wall-clock. Both phantoms were scored `false_done_referee` — i.e. they injected fake false completions into `conf_off`, which **is** the primary endpoint's ablated arm, biasing it in the direction the campaign is trying to measure. The intended benchmarks never ran: backtest 2 lost *Mine 64 NEW cobblestone*, backtest 4 lost *Craft 1 NEW iron pickaxe*. Rows retained in `exclusions`, not deleted. Not fixed in code: the repo is frozen at `7ea45ee` for the campaign and §8 forbids mixing arms across code versions. Queued for the next campaign. | 2 |
| 2026-08-13 | `conf_verify` backtest 4 | — | **Bot process died mid-arm; 5 attempts never collected, none contaminated.** After the last logged attempt the MCP endpoint on `:8765` stopped answering and the runner's preflight failed for the final 5 benchmarks of the arm. **No rows were written**, so this is a clean gap rather than a discard — verified by checking that all 8 collected rows in the arm ran against a live bot. Note the runner logs `row still logged, continuing` on this path, which is **false**: it misreports its own data loss. Fixing that message is queued for the next campaign. | 5 (never logged) |
| 2026-08-12 | `conf_verify` backtest 1 | — | **Transient `529 Overloaded` on the task-giver; one benchmark skipped, not retried.** The task-giver returned `API Error: 529 Overloaded`, no task id appeared in `queue.log`, and the runner reported `task never added (rc=43) — retry budget 1/2` but then **advanced to the next benchmark instead of retrying it**. Lost benchmark: *Craft 1 NEW furnace*. No row was written. The gap between the message and the behaviour is a runner defect queued for the next campaign. | 1 (never logged) |
| 2026-08-14 | `conf_*` backtest 2, original collection | 3 (run-level) | **Whole backtest superseded by a re-run, so the `gates` arm could carry its true arm position.** Backtest 2's randomized arm order is `verify on off perception reflexes autofinish gates`; `gates` ran **7th** and was truncated at 6 of 13 tasks by API credit exhaustion. Re-running `gates` alone was rejected because `field_trial.sh` numbers `ARM_POSITION` from 1 per invocation and would have stamped position 1 on a slot-7 arm — corrupting the covariate introduced specifically to control arm position, on the one arm carrying the campaign's unresolved 100 %-success anomaly. Reporting unequal n was rejected because that same arm is the least able to afford reduced power. The re-run is a **true replicate**: both deterministic shuffles were verified before launch to reproduce backtest 2 exactly (arm order as above, task order `3 7 12 0 5 8 1 11 4 10 9 6 2`), at the same commit `7ea45ee`, and completed all 7 arms at 13 tasks. Disposition of these rows was **pre-declared before the re-run's outcome was known** (preregistration, 2026-08-14): completion → these rows superseded; truncation → the re-run discarded instead and these retained. The two backtest-2 datasets are never pooled. Rows retained in `exclusions`, not deleted. | 77 |
| 2026-08-14 | `conf_reflexes` backtest 2 (re-run) | 3 (runner defect) | **Task-queue dedup collision, third instance — caught by an invariant check, not by a crash.** The runner requested benchmark #4 (*Craft 12 NEW sticks*) but the task-giver's add returned pre-existing queued task #1147, and the attempt ran for the full 480 s against that task's text instead: *"Place furnace, mine 5 iron ore, smelt into 5 iron ingots"*, criterion `5 iron_ingot in inventory`. Two independent invariants flag it: the `task_name` is not one of the 13 benchmark descriptions, and the criterion is an **absolute** count rather than the campaign's canonical `+N … (net gain this run)` form — so held stock could satisfy it and no per-attempt delta was measured. This variant is more dangerous than the 2026-08-13 pair because it produced a full-length, referee-labeled, non-zero-step row that looks entirely normal in isolation; only the cross-check against `benchmarks.json` exposes it. Consequence: `reflexes` backtest 2 is missing benchmark #4. Row retained in `exclusions`, not deleted. | 1 |
---

### Pre-freeze aborts, for the record

Field Trial v1 (2026-07-19) aborted runs 2–6 before a clean run 7. These predate
this protocol and are reported as pilot-phase pipeline debugging, not as
protocol-governed aborts. Rows survive in `pincercraft_evals.db` under
`task_set = bench_on_aborted_r2 … r6` (11 attempts total).

Known causes from that phase, reconstructed from the improvement log: tidy-reflex
re-trigger (fixed `8c7ac32`), the run-4 pre-spawn wedge (fixed by `wait_spawn`),
and inventory inheritance between arms.
